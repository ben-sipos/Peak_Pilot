const fs = require('fs'); const { JSDOM } = require('jsdom'); const html = fs.readFileSync('index.html', 'utf8'); const domInstance = new JSDOM(html, { runScripts: 'dangerously', resources: 'usable' }); setTimeout(() => { try { console.log('App HTML length:', domInstance.window.app.innerHTML.length); domInstance.window.state.view = 'resort'; domInstance.window.state.selectedResortId = 'zermatt'; domInstance.window.render(); console.log('After render Resort HTML length:', domInstance.window.app.innerHTML.length); } catch (e) { console.error('Error during render:', e); } }, 2000);
